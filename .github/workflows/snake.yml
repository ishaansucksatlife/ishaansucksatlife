name: Generate Snake

permissions:
  contents: write

on:
  push:
    branches: [ main ]
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

concurrency:
  group: snake-generation
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate snake files
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-snake.svg?color_snake=%2300FFFF&color_dots=%2306888B,%2394DED9,%2300FFFF,%23FE428E,%23F8D847
            dist/github-snake-dark.svg?palette=github-dark
            dist/github-snake.gif?color_snake=%2300FFFF&color_dots=%2306888B,%2394DED9,%2300FFFF,%23FE428E,%23F8D847

      - name: List generated files
        run: ls -l dist

      - name: Deploy to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
          create_repo: true
          commit_message: "chore: update snake animation [skip ci]"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
